import { Grid } from "@mui/material";
import React from "react";

const KpiCards = () => {
  const data = [{}];

  return (
    <Grid container>
      <Grid item></Grid>
    </Grid>
  );
};

export default KpiCards;
